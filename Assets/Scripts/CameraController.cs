using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{


    //PlayerController controls;


    private void Awake()
    {
        //controls.Movement.RSMove.performed += ctx => passAxis(ctx.ReadValue<Vector2>()) ;
    }

    void Update()
    {
       
    }

    private void passAxis(Vector2 vector)
    {

    }
}
